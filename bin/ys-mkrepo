#!/usr/bin/env bash

if [ $# -ne 1 ]; then
  echo "usage:" 1>&2
  echo "${0} <repo name (without .git)>" 1>&2
  exit 1
fi

repodir='/work/git'
target="${1%.git}"

repository="${repodir}/${target}.git"
mkdir -pv       "${repository}"
git init --bare "${repository}"

