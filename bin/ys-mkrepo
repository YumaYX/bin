#!/usr/bin/env bash

if [ $# -ne 1 ]; then
  echo "usage:" 1>&2
  echo "${0} <repo name (without .git)>" 1>&2
  exit 1
fi

repodir='/work/git'
target="${1%.git}"

repository="${repodir}/${target}.git"
echo "${repository}"
mkdir -p "${repository}"
cd "${repository}" && git init --bare

