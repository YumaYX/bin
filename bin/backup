#!/usr/bin/env bash

target='/work'
ls "${target}" > /dev/null || exit 1

backup_file='works.tar.gz'
backup_full_path="${target}/${backup_file}"

tmp="/tmp/${backup_file}"

tar zcvf "${tmp}" ${target}/* \
  && rm -f "${backup_full_path}" \
  || cp -v ${tmp} ${target}
