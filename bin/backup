#!/usr/bin/env bash

target='/work'
ls "${target}" > /dev/null || exit 1

backup_full_path="${target}/works.tar.gz"
backup_file=$(basename "${backup_full_path}")

tmp="/tmp/${backup_file}"

tar zcvf "${tmp}" ${target}/* \
  && rm -f "${backup_full_path}" \
  || cp -v ${tmp} ${target}
