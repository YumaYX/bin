#!/usr/bin/env ruby
# frozen_string_literal: true

require 'ipaddr'

original_ip = IPAddr.new('255.255.255.255/32')

(0..32).each do |mask_length|
  masked_ip = original_ip.mask(mask_length)
  puts "#{mask_length}\t#{masked_ip}"
end
